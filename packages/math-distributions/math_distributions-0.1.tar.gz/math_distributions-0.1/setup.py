from setuptools import setup

setup(name='math_distributions',
      version='0.1',
      description='Gaussian and Binomail distributions',
      packages=['math_distributions'],
      zip_safe=False)
