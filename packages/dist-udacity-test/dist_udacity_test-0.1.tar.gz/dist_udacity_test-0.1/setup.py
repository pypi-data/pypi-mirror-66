from setuptools import setup

setup(name='dist_udacity_test',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['dist_udacity_test'],
      Author = "Luis Rocha",
      zip_safe=False)
