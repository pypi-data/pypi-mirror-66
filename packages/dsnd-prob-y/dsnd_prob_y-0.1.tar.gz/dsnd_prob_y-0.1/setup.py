from setuptools import setup

setup(name='dsnd_prob_y',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages = ['dsnd_prob_y'],
      author = 'Yufei GE',
      zip_safe=False)
