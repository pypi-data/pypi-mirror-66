from setuptools import setup

setup(name='mleng_distributions',
      version='0.1',
      description='Gaussian & Binomial distributions',
      packages=['mleng_distributions'],
      author='Adrian R',
      zip_safe=False)
