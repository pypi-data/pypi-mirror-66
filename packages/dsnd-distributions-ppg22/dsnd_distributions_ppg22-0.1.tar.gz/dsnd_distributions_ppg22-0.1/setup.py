from setuptools import setup

setup(name='dsnd_distributions_ppg22',
      version='0.1',
      description='Gaussian and binomial distributions',
      packages=['dsnd_distributions_ppg22'],
      zip_safe=False)
