from setuptools import setup

setup(name='mleap_probability',
      version='1.2',
      description='Gaussian and Binomial distributions',
      packages=['mleap_probability'],
      author=['Anna Pastwa'],
      zip_safe=False)
