from setuptools import setup

setup(name='gaussian_binomial_pande',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions'],
      author = 'Kartik Pande',
      zip_safe=False)
